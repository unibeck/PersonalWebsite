ci:
  collect:
    additive: false
    start-server-command: gatsby serve
    start-server-ready-pattern: "localhost:9000"
    url: http://localhost:9000/
    settings:
      chromeFlags: --no-sandbox
  upload:
    target: filesystem
    outputDir: .lighthouseci
